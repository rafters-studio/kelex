pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm oxlint {staged_files}
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    flightcheck:
      run: pnpm flightcheck
