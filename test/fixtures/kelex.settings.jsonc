{
  // Settings for the CLI spec: the default plugins, resolved from the repo root
  // above this directory, with an action the tests can recognize.
  "renderer": "@kelex/plugin-renderer-html",
  "handler": "@kelex/plugin-handler-post",
  "renderer.options": { "action": "/fixture-submit" },
}
